{
  "name": "pitchside-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "On-chain USDT escrow for PitchSide peer-to-peer match betting (BSC).",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "hardhat compile && npm run export-abi",
    "export-abi": "node scripts/export-abi.js",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
